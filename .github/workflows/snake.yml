name: GenerateGithubContributionGridSnakeSvg
on:
  push:
    branches:
      - main  # 这里表示只有 main 分支有推送操作时触发工作流，你可以根据实际需要修改为其他分支或者设置为 ['*'] 表示任意分支推送都触发
  pull_request:  # 当有拉取请求时也触发工作流
  schedule:
    - cron: "0 0 * * *"  # 每天凌晨（零点）定时触发工作流，格式为：分 时 日 月 周，这里表示每天 0 分 0 时触发

jobs:
  generate_snake:
    runs-on: ubuntu-latest

    steps:
      - name: Generate SVG Files
        uses: Platane/snk/svg-only@v3
        with:
          # github user name to read the contribution graph from (**required**)
          # using action context var `github.repository_owner` or specified user
          github_user_name: ${{ github.repository_owner }}

          # list of files to generate.
          # one file per line. Each output can be customized with options as query string.
          #
          #  supported options:
          #  - palette:     A preset of color, one of [github, github-dark, github-light]
          #  - color_snake: Color of the snake
          #  - color_dots:  Coma separated list of dots color.
          #                 The first one is 0 contribution, then it goes from the low contribution to the highest.
          #                 Exactly 5 colors are expected.
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
            dist/ocean.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9